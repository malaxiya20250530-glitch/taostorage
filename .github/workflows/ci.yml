name: CI — 道可道

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: 🦀 Build tao CLI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: 🔨 Build
        run: cargo build --release --bin tao
      - name: 🧪 Test core
        run: cargo test --package tao-core
      - name: 📤 Upload
        uses: actions/upload-artifact@v4
        with:
          name: tao-linux-x86_64
          path: target/release/tao
